# See scripts/performance_model.py for how these numbers are generated
# e.g. ./scripts/performance_model.py --model matmul scripts/perf_data/matmul-op-performance.csv

# See scripts/performance_model.py for how these numbers are generated
# e.g. ./scripts/performance_model.py --model eltwise scripts/perf_data/sqrt-op-performance.csv

default: # same as nop
  ublock_weight: 305
  tile_weight: 56

add:
  ublock_weight: 446
  tile_weight: 92

subtract:
  ublock_weight: 458
  tile_weight: 96

multiply:
  ublock_weight: 430
  tile_weight: 99

exp:
  ublock_weight: 39
  tile_weight: 700

exp_approx:
  ublock_weight: 39
  tile_weight: 357

gelu:
  ublock_weight: 64
  tile_weight: 286

gelu_derivative:
  ublock_weight: 0
  tile_weight: 3116

gelu_derivative_approx:
  ublock_weight: 0
  tile_weight: 1500

log:
  ublock_weight: 0
  tile_weight: 1413

nop:
  ublock_weight: 305
  tile_weight: 56

buffer:
  ublock_weight: 305
  tile_weight: 56

reciprocal:
  ublock_weight: 29
  tile_weight: 915

reciprocal_approx:
  ublock_weight: 29
  tile_weight: 606

sigmoid:
  ublock_weight: 183
  tile_weight: 168

sqrt:
  ublock_weight: 195
  tile_weight: 159

reduce-r:
  ublock_weight: 76
  tile_weight: 59

reduce-c:
  ublock_weight: 0
  tile_weight: 91

reduce-z:
  ublock_weight: 0
  tile_weight: 287
  z_weight: 268

splice:
  ublock_weight: 132
  tile_weight: 69

depthwise:
  math_tiles_weight: 79
  math_dest_spill_weight: 0
  unpack_ublocks_weight: 0
  unpack_tiles_weight: 0

matmul:
  # V1 weights
  m_k_weight: 0
  ublock_weight: 289
  tile_weight: 18
  ublock_weight_unpack_bottlenecked: 174
  tile_weight_unpack_bottlenecked: 51
  # V2 roofline model weights
  math_tiles_weight: 15.88244012
  math_dest_spill_weight: 24.61998068
  unpack_ublocks_weight: 191.49218656
  unpack_tiles_weight: 2.15404948
  # Sparse weights
  sparse_reload: 20
  sparse_reload_config: 20
  sparse_reload_wait_pop: 20
  sparse_pack_wait_push: 25
  sparse_pack: 25
  sparse_ublock_decode: 8
  sparse_nz_ublock_decode: 8
  sparse_nz_tile_decode: 12